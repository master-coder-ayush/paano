# Spaces

Spaces belong to a brand workspace and contain name, website, description, industry, ICP, target locations, CTA URL, and status. `GET /api/brand/spaces` lists only the authorized workspace; `POST` creates or updates after workspace authorization and required-name validation.
