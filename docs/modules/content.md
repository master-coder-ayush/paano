# Content

Sprint 3 content is source-controlled and localized through `messages/en.json`. The public website does not introduce database tables or CMS dependencies. This keeps content reviewable in code while the product validates the marketplace workflow.

Future blog, report, case-study, and free-tool records should add an explicit content model with publication status, locale, slug, canonical URL, and ownership before becoming database-backed.
