# Public script

`GET /n.js` is a public, cacheable JavaScript bootstrap. It defines the `window.paano` queue function and does not expose workspace data. The Sprint 2 setup snippet uses `data-site="<public-site-key>"`; queued events are sent by the installed script in a later client-runtime iteration.
