# Results

Brand results now include a first-party funnel: click → lead → signup/trial → purchase, plus attributed conversions. Counts exclude debug and bot events. The funnel is workspace-scoped and uses normalized event/attribution records rather than raw PII.
